# Руководство по безопасности

## Автоматическая настройка безопасности

### Быстрая настройка

```bash
chmod +x scripts/security.sh
sudo ./scripts/security.sh
```

Скрипт автоматически настроит все компоненты безопасности.

## Что настраивается

### 1. Firewall (UFW) ✅

- Блокировка всех входящих соединений по умолчанию
- Разрешение только необходимых портов:
  - 22 (SSH)
  - 80 (HTTP)
  - 443 (HTTPS)
- Автоматическое включение firewall

**Проверка:**
```bash
ufw status
```

**Добавление порта:**
```bash
ufw allow 8080/tcp comment 'Custom port'
```

### 2. Fail2ban (Защита от брут-форса) ✅

- Автоматическая блокировка IP после неудачных попыток входа
- Защита SSH (3 попытки = бан на 2 часа)
- Защита от DDoS атак на SSH
- Защита Nginx

**Настройки:**
- SSH: 3 попытки → бан на 2 часа
- SSH DDoS: 10 попыток за 10 минут → бан на 1 час
- Nginx: 10 попыток → бан

**Проверка:**
```bash
fail2ban-client status
fail2ban-client status sshd
```

**Разбан IP:**
```bash
fail2ban-client set sshd unbanip <IP_ADDRESS>
```

### 3. Автоматические обновления безопасности ✅

- Автоматическая установка обновлений безопасности
- Ежедневная проверка обновлений
- Автоматическая очистка старых пакетов

**Проверка:**
```bash
systemctl status unattended-upgrades
cat /var/log/unattended-upgrades/unattended-upgrades.log | tail -20
```

### 4. SSH Безопасность ✅

- Отключение root логина по паролю (опционально)
- Ограничение количества попыток входа (3)
- Таймаут неактивных сессий (5 минут)
- Отключение X11 forwarding
- Отключение DNS lookup

**Важно:** Если отключили root логин, убедитесь, что:
1. У вас есть SSH ключ
2. Есть другой пользователь с sudo правами

**Проверка конфигурации:**
```bash
sshd -T | grep -E "permitrootlogin|maxauthtries|clientalive"
```

### 5. Ограничения ресурсов ✅

- Увеличение лимитов открытых файлов (65536)
- Увеличение лимитов процессов (32768)

**Проверка:**
```bash
ulimit -a
```

### 6. Параметры ядра (sysctl) ✅

- Защита от SYN flood атак
- Защита от IP spoofing
- Отключение ICMP redirects
- Отключение source routing
- Логирование подозрительных пакетов

**Проверка:**
```bash
sysctl -a | grep -E "tcp_syncookies|rp_filter|log_martians"
```

### 7. Auditd (Аудит системы) ✅

- Мониторинг изменений в системных файлах
- Мониторинг изменений в конфигурациях
- Логирование системных вызовов

**Просмотр логов:**
```bash
ausearch -k security
ausearch -k identity
ausearch -k sshd_config
```

### 8. AppArmor ✅

- Включение AppArmor для дополнительной защиты
- Профили безопасности для приложений

**Проверка:**
```bash
apparmor_status
```

### 9. Логирование ✅

- Настройка ротации логов
- Централизованное хранение логов безопасности

**Просмотр логов:**
```bash
tail -f /var/log/auth.log
tail -f /var/log/syslog
```

### 10. Отключение ненужных сервисов ✅

Автоматически отключаются:
- Bluetooth
- CUPS (принтеры)
- Avahi (mDNS)

## Мониторинг безопасности

### Скрипт проверки

После установки доступен скрипт для проверки:

```bash
/usr/local/bin/security-check.sh
```

Он показывает:
- Статус firewall
- Статус fail2ban
- Последние неудачные попытки входа
- Активные SSH сессии
- Статус автоматических обновлений
- Открытые порты

### Ручная проверка

**Проверка подозрительной активности:**
```bash
# Неудачные попытки входа
grep "Failed password" /var/log/auth.log | tail -20

# Успешные входы
grep "Accepted password" /var/log/auth.log | tail -20

# Забаненные IP
fail2ban-client status sshd
```

**Проверка открытых портов:**
```bash
ss -tulpn | grep LISTEN
netstat -tulpn | grep LISTEN
```

**Проверка активных соединений:**
```bash
ss -tnp
netstat -tnp
```

## Дополнительные меры безопасности

### 1. Настройка SSH ключей

**Генерация ключа (на локальном компьютере):**
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

**Копирование ключа на сервер:**
```bash
ssh-copy-id user@server-ip
```

**Отключение парольной аутентификации (после настройки ключей):**
```bash
sudo nano /etc/ssh/sshd_config
# Установите:
PasswordAuthentication no
PubkeyAuthentication yes

sudo systemctl restart sshd
```

### 2. Настройка двухфакторной аутентификации (2FA)

**Установка Google Authenticator:**
```bash
apt-get install libpam-google-authenticator
google-authenticator
```

**Настройка PAM:**
```bash
echo "auth required pam_google_authenticator.so" >> /etc/pam.d/sshd
```

### 3. Настройка мониторинга

**Установка и настройка мониторинга (опционально):**
- Prometheus + Grafana
- Zabbix
- Nagios

### 4. Регулярные проверки

**Ежедневные задачи:**
```bash
# Добавить в crontab
0 9 * * * /usr/local/bin/security-check.sh | mail -s "Daily Security Check" admin@example.com
```

### 5. Резервное копирование

Настройте автоматическое резервное копирование:
```bash
# Используйте скрипт backup.sh
/opt/proxy/scripts/backup.sh
```

## Рекомендации

### Обязательно:

1. ✅ Регулярно обновляйте систему
2. ✅ Используйте сильные пароли
3. ✅ Настройте SSH ключи
4. ✅ Регулярно проверяйте логи
5. ✅ Настройте мониторинг

### Рекомендуется:

1. ⭐ Настроить двухфакторную аутентификацию
2. ⭐ Настроить централизованное логирование
3. ⭐ Настроить алерты на подозрительную активность
4. ⭐ Регулярно проводить аудит безопасности
5. ⭐ Использовать VPN для доступа к серверу

### Опционально:

1. 🔹 Настроить IDS/IPS (Intrusion Detection/Prevention System)
2. 🔹 Настроить WAF (Web Application Firewall)
3. 🔹 Настроить DDoS защиту
4. 🔹 Провести пентест

## Устранение неполадок

### Проблема: Заблокирован доступ по SSH

**Решение:**
```bash
# Подключитесь через консоль провайдера или KVM
# Разбаньте свой IP:
fail2ban-client set sshd unbanip YOUR_IP

# Или временно отключите fail2ban:
systemctl stop fail2ban
```

### Проблема: Firewall блокирует нужный порт

**Решение:**
```bash
# Добавьте правило:
ufw allow PORT/tcp comment 'Description'

# Или временно отключите firewall (НЕ рекомендуется):
ufw disable
```

### Проблема: Не работают автоматические обновления

**Решение:**
```bash
# Проверьте статус:
systemctl status unattended-upgrades

# Проверьте логи:
tail -f /var/log/unattended-upgrades/unattended-upgrades.log

# Запустите вручную:
unattended-upgrade --dry-run
```

### Проблема: Слишком много ложных срабатываний fail2ban

**Решение:**
```bash
# Увеличьте количество попыток в /etc/fail2ban/jail.local:
maxretry = 10

# Перезапустите:
systemctl restart fail2ban
```

## Логи безопасности

**Основные файлы логов:**
- `/var/log/auth.log` - SSH и аутентификация
- `/var/log/syslog` - Системные события
- `/var/log/fail2ban.log` - Fail2ban события
- `/var/log/audit/audit.log` - Auditd логи
- `/var/log/proxy_security_setup.log` - Лог установки

**Просмотр в реальном времени:**
```bash
tail -f /var/log/auth.log
tail -f /var/log/fail2ban.log
```

## Проверка безопасности

### Быстрая проверка

```bash
/usr/local/bin/security-check.sh
```

### Полная проверка

```bash
# 1. Проверка firewall
ufw status verbose

# 2. Проверка fail2ban
fail2ban-client status

# 3. Проверка обновлений
apt list --upgradable

# 4. Проверка открытых портов
ss -tulpn

# 5. Проверка активных пользователей
who
last

# 6. Проверка подозрительной активности
grep "Failed password" /var/log/auth.log | wc -l
```

## Обновление настроек безопасности

Если нужно обновить настройки:

```bash
# Запустите скрипт снова
sudo ./scripts/security.sh
```

Или обновите конкретные компоненты вручную.

## Дополнительные ресурсы

- [Ubuntu Security Guide](https://ubuntu.com/security)
- [Fail2ban Documentation](https://www.fail2ban.org/)
- [SSH Hardening Guide](https://stribika.github.io/2015/01/04/secure-seed-shells.html)

---

**Важно:** Безопасность - это процесс, а не разовое действие. Регулярно проверяйте и обновляйте настройки безопасности!
