# Быстрая установка (Quick Install)

## Для нетерпеливых 🚀

### Шаг 1: Подготовка

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Скачайте проект
cd /tmp
git clone <repository-url> proxy
# или распакуйте архив
```

### Шаг 2: Запуск установки

```bash
cd proxy
chmod +x scripts/install.sh
./scripts/install.sh
```

### Шаг 3: Настройка SSL (после установки)

```bash
/opt/proxy/scripts/setup_ssl.sh your-domain.com your-email@example.com
```

### Готово! ✅

Откройте в браузере:
- `https://your-domain.com/docs` - API документация
- `https://your-domain.com/health` - Проверка здоровья

**Учетные данные администратора:**
- Email: `admin@example.com`
- Password: `admin123`
- ⚠️ **Смените пароль сразу после первого входа!**

---

## Что дальше?

1. **Смените пароль администратора** через API или напрямую в БД
2. **Создайте пользователей** через `/api/register`
3. **Настройте мониторинг** (опционально)
4. **Изучите API** на `/docs`

---

## Нужна помощь?

- **Подробная инструкция**: [INSTALL.md](INSTALL.md)
- **Документация API**: [API.md](API.md)
- **Развертывание**: [DEPLOY.md](DEPLOY.md)

---

## Полезные команды

```bash
# Статус сервиса
systemctl status proxy

# Логи приложения
cd /opt/proxy && docker-compose logs -f app

# Перезапуск
systemctl restart proxy

# Бэкап БД
/opt/proxy/scripts/backup.sh
```
